// The operator's contact points. One place for the strings the app
// repeats — new surfaces should import these rather than minting
// another literal (several older pages still carry their own copies).
export const WHATSAPP_NUMBER = "919142349007";
export const SUPPORT_EMAIL = "crikledger@gmail.com";
