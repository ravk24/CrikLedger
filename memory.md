# Memory — session 27: team ledger export as XLSX

Last updated: 2026-09-09 (session 27, end)

## What was built

- **`GET /api/export/ledger`** (`app/api/export/ledger/route.ts`): the whole team ledger as one `.xlsx` workbook — **Ledger** (oldest first, running balance = cumulative sum of stored amounts, ends on `pool_balance`), **Balances** (dashboard order, plus a `Sum of player balances` row — deliberately not called the pool balance), **Statements** (every player's `player_statement` rows, contiguous per player, `running_balance` from the view). Rows 1–4 are a header block (team, export time in IST, pool balance, row count); row 6 is frozen and filtered. Guard `requireTeamAdmin()`; failures keep the JSON envelope (guest 401, megaadmin 403 `MEGAADMIN_READ_ONLY`, other team 403). `Content-Disposition: attachment; filename="<slug>-ledger-<yyyy-mm-dd>.xlsx"`, `Cache-Control: no-store`.
- **Reads go through `pg`, not supabase-js.** PostgREST caps one request at 1000 rows whatever `.range()` asks for, and an export is whole-history. `entry_date::text` / `created_at::text` in SQL so node-postgres never turns a tz-less DATE into a local-midnight Date. Caps: 5000 ledger rows, 20000 statement rows — one row past the cap marks the sheet INCOMPLETE in red rather than failing.
- **`lib/export/ledgerRows.ts`**: pure shaping (`withRunningBalance`, `sortBalances`, `shapeStatements`, `ledgerTitle`, `matchLabel`, `kindLabel`, `isoDateToUtc`, `exportFilename`, `exportedAtLabel`). Neutral `Export*Row` types so a tournament export can reuse them. Amounts are `NUMERIC(10,2)`, so pg returns strings — everything passes through `Number()`.
- **`lib/export/ledgerWorkbook.ts`**: ExcelJS builder. `#,##0;[Red]-#,##0` on money, `dd-mmm-yyyy` on real date cells built at UTC midnight.
- **`components/shared/DownloadImageButton.tsx`**: the body became a generic `DownloadFileButton` (`label`, `mimeType`, `icon`, `errorText` props); `DownloadImageButton` keeps its signature and delegates, so the three PNG call sites are untouched. A failed fetch now surfaces the envelope's `error.message`. **Owner ask, same session:** the image flavour now shows `Share2` with the label "Share X image" (the images exist to go into the WhatsApp group; the match-sheet and guest buttons already used that glyph). The Excel button keeps `FileSpreadsheet` + "Download", since Android refuses `.xlsx` in the share sheet. **Bug found on the owner's phone after deploy:** the spreadsheet tap showed "Permission denied" — Chrome's `canShare()` in the page said yes, then the share dialog rejected `.xlsx` with `NotAllowedError`, and the button surfaced that as the error. Fix: `share={false}` on the spreadsheet call site (straight to `<a download>`), and in the share branch a `NotAllowedError` (also raised when the tap's user activation expires during a slow cold-function fetch) now falls through to the download instead of an error.
- **`components/pool/PoolAdminSection.tsx`**: toolbar is `grid-cols-[1fr_1fr_auto_auto]` — Credit, Debit, share image, **Excel export** (`FileSpreadsheet`). New `teamSlug` prop from `app/(app)/pool/page.tsx`.
- **`next.config.ts`**: `serverExternalPackages: ["exceljs"]`. `package.json`: `exceljs@4.4.0` (runtime). Never `xlsx`/SheetJS from npm (spec rule).
- Tests: `lib/export/ledgerRows.test.ts` (13) and `ledgerWorkbook.test.ts` (3, round-trips the file through `workbook.xlsx.load`). Suite 92 → **108**.
- Docs: `CrikLedger-docs/09-api.md` §2.11 + summary table (48 files / 58 methods), `02-feature-inventory.md` §I row struck through, `README.md` (Export paragraph, `/pool` row), `context/ui-registry.md` (DownloadFileButton entry).

- **Backups (`.github/workflows/db-backup.yml`, `db/BACKUP.md`)**: the owner had lost `BACKUP_PASSPHRASE`; they overwrote the Production environment secret (16:58Z) and manual run #14 went green with it. Then retention moved from a 30-day window to **count-based: keep the newest 7** — a final "Keep only the newest 7 backups" step (`gh api` list → `sort -r` → `tail -n +8` → DELETE) with `actions: write` on the job token; `retention-days` raised to 90 as a backstop only. Runs after upload, so a failed run never prunes. Manual runs count. The repo is private, not public as the runbook said.

- **Team viewer (migration 49, same session, after a feasibility ask):** one shared read-only login per team. `team_role` CHECK admits `'viewer'`; partial unique `team_memberships_one_viewer_key` (one active per team); no admin slot (`tm_admin_has_slot` forces NULL). **The trap:** `canWrite` in `lib/roles.ts` granted any membership row without reading the role — now `role !== "viewer"`; `isViewer(p)` added; 7 new roles tests. `lib/session.ts` `requireScope` / `requireTournamentWrite` answer `403 VIEWER_READ_ONLY` (`viewerReadOnlyError()`), so every write, share image, export and `change-password` inherit it. **Owner amendment mid-build: ONE viewer at a time (migration 50).** `admins.viewer_session_started_at` is the seat; `login` claims it atomically after the password check (`claimViewerSeat`) and answers `409 VIEWER_BUSY` naming the team's superadmin and the time the seat was taken; `logout` bumps the epoch AND frees the seat for every account (so the earlier "skip the bump for viewers" was reverted); the superadmin's sign-out / reset / remove free it too. Never freed by time — the message tells the player to ask the superadmin or the signed-in player. `GET /api/sa/viewer` returns `in_use_since`; the card shows it and greys "Sign out the viewer" while free. `formatDateTime()` added to `lib/format.ts`. New routes `app/api/sa/viewer` (GET / POST `{username, password}` → `{username}` / DELETE), `viewer/signout`, `viewer/reset-password`; **the superadmin picks the user id** (owner amendment — a derived `<slug>_viewer` was built first and dropped; `createViewerSchema` = `usernameField` + password), account named `"<team> viewer"`, `must_change_password FALSE`, password chosen by the superadmin, never returned. `/api/sa/admins` refuses to link a viewer (`409 VIEWER_ACCOUNT`) and excludes it from GET; `/api/ops/accounts` grant picker excludes viewer accounts. `NavState` gained `isViewer` + `canWriteActiveTeam`. Six session-presence gates now use `canWrite`/`isViewer`: Home share button, `/pool` admin section, `/schedule` tile (hidden for viewers), `/admin` + `/admin/players` (redirect `/`), `TournamentTabBarGate`; plus `/admin/password` and `/purchases` redirect viewers, More hides Purchases, AccountMenu hides Team admin / Purchases / Change password. New `components/admin/ViewerManager.tsx` on `/admin/manage`. Constraint name verified on prod before writing the migration: `team_memberships_team_role_check`.

## Decisions made

- Scope v1 = team ledger + balances + statements in one workbook; tournament export later; XLSX only (no CSV); button on `/pool` only (no More card); any admin of the team may export.
- ExcelJS's `index.d.ts` declares a global `interface Buffer extends ArrayBuffer` that nothing real satisfies — two commented casts (`writeBuffer()` result → `Uint8Array`, test `load()` argument). Do not "fix" by changing tsconfig.
- Android Chrome refuses `.xlsx` in `navigator.share({files})`; the button falls through to `<a download>` there. Copy never promises a share sheet for the spreadsheet.

## Verified

- **Team viewer, end to end on the dev server against prod data (2026-09-09 late):** migrations 49 and 50 applied (each dry-run in `BEGIN…ROLLBACK` first; backup run 17 before 49). Driving the owner's Chrome as superadmin: create (`POST /api/sa/viewer` 201 with a chosen user id), Sign out the viewer (200), Reset password (200), Remove (DELETE 200). With curl as the viewer: login 200 → second login `409 VIEWER_BUSY` "already in use since 09 Sept, 11:33 pm. Ask Ravi Kant (superadmin)…"; wrong password stays `INVALID_CREDENTIALS`; every write, both share images, the export, change-password and every `/api/sa/*` route → `403 VIEWER_READ_ONLY`; `/`, `/pool`, `/schedule`, `/schedule/upcoming`, `/car-count`, `/more` 200 with no Credit/Debit, share, export or schedule controls and no Purchases tile; `/admin`, `/admin/players`, `/admin/password`, `/purchases` stream `NEXT_REDIRECT … /` with no console content (curl shows 200 because the redirect fires inside the Suspense boundary — the browser lands on Home); viewer logout frees the seat and kills its cookie; superadmin sign-out kills the cookie while the same password still logs in; reset kills the cookie and the old password; removed login → `403 ADMIN_REVOKED`. **Gotchas met:** the browser tab kept a stale client bundle after each edit (hydration mismatch in the dev log) until a hard reload; a third-party Chrome extension ("BHK widget") swallowed clicks and typing on the manage page, so form values were set by element reference. **Residue on prod:** two deactivated test accounts, `ravi_kant_sgsa_viewer` (first attempt, derived id) and `sgsa_viewer_test` — both `is_active = FALSE`, memberships revoked; safe to `DELETE FROM admins` by hand if the owner wants the user ids back.
- Export: `npx tsc --noEmit` clean, `npm test` 108/108, `npm run lint` at the 12 pre-existing warnings, `npm run build` clean with `/api/export/ledger` in the route manifest.
- `next start` + `curl /api/export/ledger` without a cookie → `401 {"success":false,"error":{"code":"UNAUTHORIZED",…}}`, `application/json`.
- The four SQL statements run read-only against prod for the first team (54 ledger rows, 21 players, 33 statement rows): column names match the `*Source` types, dates arrive as strings, and the ledger sum (8883) equals `pool_balance` — the running balance will reconcile.
- **Not exercised:** the signed-in download in a browser (no admin credentials in-session), the Android/iOS share paths, and the function size on Vercel after deploy.

## Current state

- **Git:** export (`27363b3`), share icon + Android fix (`8bdb608`), backup retention (`2e0f4ca`), backup apt hardening (`5aa6e1f`) are pushed. **The team viewer (migrations 49–50, roles, routes, gates, ViewerManager, docs) is uncommitted on `main`** at the time of writing; both migrations are already applied to prod, so `main` on Vercel is behind the schema until this is pushed (harmless: nothing in the deployed code reads the new column or role).

## Next session starts with

1. If not done at the end of session 27: commit the team viewer (`feat(auth): team viewer — one shared read-only login per team, one seat`) + notes, push, and on the phone: create a real viewer from Manage admins, share it in the group, watch a second sign-in get the "already in use" message.
2. Check the Excel export on the phone (plain download, opens in Sheets) and the ExcelJS function size on the Vercel deployment page.
3. Decide whether the tournament Ledger tab gets the export button (`lib/export/*` is shaped for it) and whether a viewer should ever see the tournament share image (today: no, `VIEWER_READ_ONLY`).
4. Optional cleanup: `DELETE FROM admins WHERE username IN ('ravi_kant_sgsa_viewer','sgsa_viewer_test')` (both deactivated test rows; memberships cascade).
4. Carried from session 26: browser check of the delete-from-ledger flow; known issue 10.10 (abandon and completed-match DELETE still orphan `pending_cleared_entry_id`).
5. Carried from 2026-08-22: **rotate the DB password** (`pending-tasks.md` item 6). Also strip the plaintext password and backup passphrase reported at the top of `Project Details/notes/cricLedger.txt` (git-ignored, but on disk).

## Open questions

- Should the tournament schedule tab's "Scheduled" card also become "Matches"? (carried)
- Does anyone need CSV, or is XLSX enough? Left out on purpose.
